# Versions selected for Python 3.14 compatibility (Sep 2026).
# Keep uvicorn free of [standard] extras so startup does not require native compilers.
fastapi==0.141.1
uvicorn==0.35.0
httpx==0.28.1
pydantic==2.13.5
# Planning engines: LP, sparse DC power flow, graph criticality.
numpy==2.5.3
scipy==1.18.1
networkx==3.5
orjson==3.12.0
