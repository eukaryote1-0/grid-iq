# Current state

## Current status

Implemented and locally validated:
- responsive control-centre UI with page/scenario/selection persistence;
- FastAPI preferred runtime plus dependency-free stdlib fallback;
- Python 3.14-compatible dependency/launcher path;
- live OSM/Overpass power geometry connector and geometry-topology summary;
- topology visualisation, voltage-class rendering, connected-component/end-point/junction metrics;
- OSM-line enrichment with documented PyPSA/PyPSA-Earth benchmark R/X/current/secure-capacity assumptions for mapped 132/220/400 kV classes;
- conservative/reference/high-capacity sensitivity cases;
- dependency-free single-voltage DC transfer sensitivity with separate approximate I²R loss post-processing;
- live NASA POWER point resource query;
- live WorldPop v2 population query around a selected coordinate;
- Statistics Botswana Q1 2026 electricity baseline, SAPP system-capacity snapshot and Botswana National Energy Compact snapshot;
- Statistics Botswana 2022 Census district population and NEUS 2022/23 access/renewable/non-connection tables;
- deterministic Census×NEUS district access-pressure index, explicitly not a count of unconnected people/households and not MW load;
- IRENA/NREL BESS benchmark layer;
- historical WRI Botswana plant reference;
- SHA-256 manifest for bundled evidence/reference/benchmark/derived files;
- repo collaboration scaffolding (`CONTRIBUTING.md`, `AGENTS.md`, CI, Makefile) and committed external datasets with manifest verification;
- **E2 national transmission loss reconciliation** (`engines/e2_flow/`, `/api/engines/loss-reconciliation`): scipy sparse DC power flow over the filtered OSM 132/220/400 kV network with substation demand allocation, reconciled against BPC 642 GWh / AFREC 630 GWh;
- evidence-gated audit with separate public-data-planning, benchmark-engineering and BPC-operational-validation scores.

## Current audit position

Current computed scores from explicit gates:
- UI/application: 7.3/10 (current release browser regression, local vendor assets and formal accessibility audit remain unverified/missing);
- open-data governance: 10.0/10;
- benchmark engineering capability: 10.0/10 for the implemented benchmark-sensitivity scope;
- public-data planning product: 9.2/10;
- BPC operational validation: 0.0/10 because authoritative BPC electrical/load validation data are absent;
- whole solution including operational validation: 7.3/10.

These scores are generated by `app.logic.audit_payload()` from named boolean evidence gates; they are not typed into the UI.

## Current blockers

- No validated BPC bus/branch reconciliation, line ratings, conductor/circuit records, impedances, transformer parameters or switch state.
- No time-aligned BPC feeder/substation loads and injections.
- No validated AC power flow or N-1 study.
- Global Solar/Wind Atlas rasters, Meta HR population and authoritative current Botswana boundary geometry are not physically bundled/ingested.
- Leaflet and Chart.js remain public-CDN runtime dependencies.
- Live OSM/NASA/WorldPop calls depend on public upstream availability.
- Current v1.4 browser E2E could not be executed in the sandbox because the installed Chromium build hangs even on a data URL and Playwright browser binaries are absent. Do not claim the current release browser fixture passed.

## Known broken functionality

No known failure in bundled data/API/model paths after unit/API and runtime smoke tests. External services can be unavailable; UI/API paths must expose degraded states rather than generating substitute values.

## Validation performed

- `pytest -q`: 27 passed.
- `python -m py_compile app/*.py tests/*.py`: pass.
- `node --check web/js/app.bundle.js`: pass.
- FastAPI runtime health, audit, benchmark and derived-demand endpoint smoke tests: pass.
- stdlib compatibility runtime health, audit, benchmark and derived-demand endpoint smoke tests: pass after fixing a missing compatibility-server import.
- security headers/CSP inspected over live FastAPI HTTP: pass.
- current-release browser E2E: NOT VERIFIED for the environment limitation above.

## Immediate next steps

1. Run `scripts/browser_smoke.sh` (or Playwright E2E) on a workstation with a working Chromium/Chrome and update the audit gate only after it passes.
2. Vendor Leaflet and Chart.js and run formal accessibility checks to raise the UI score.
3. Ingest Solar/Wind Atlas rasters plus authoritative boundaries and spatially join Census/NEUS/WorldPop/resource/grid layers.
4. Obtain BPC electrical/load data contract fields before calling any output operational PF/OPF/congestion.
5. Move the evidence substrate to PostGIS/tiles and E1/E2 workers only when the hackathon prototype requires that scale.
