## What changed

<!-- One paragraph. Reference the increment (I0, I1, I2, ...) or issue. -->

## Evidence impact

- [ ] No new data claims, **or** every new number carries an evidence class and source.
- [ ] No synthetic operational series introduced.
- [ ] BPC operational-validation audit gate left `false` unless real utility data landed.

## Verification

- [ ] `make test` passes
- [ ] `./run.sh` starts and `/api/health` responds
- [ ] `make verify` passes (if data changed)

## Notes / assumptions

<!-- List anything assumed, and where it is labelled in code. -->
